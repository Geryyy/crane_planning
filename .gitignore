build/
install/
log/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# Nothing is code-generated into the source tree any more; the exporter compiles
# into `ocp.CACHE`. Left ignored so an old checkout's tree does not reappear.
generated/
# MuJoCo appends its warnings here, in whatever directory a run started in.
MUJOCO_LOG.TXT
